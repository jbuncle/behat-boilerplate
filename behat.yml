#behat.yml
default: 
    extensions:
        Behat\MinkExtension:
            goutte: ~
    suites:
        default:
            paths:
                - %paths.base%/features
            contexts: [ Dada\Behat\FeatureContext ]
